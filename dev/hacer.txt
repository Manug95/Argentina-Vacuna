que el usuario del deposito que pide las vacunas elija a que deposito nacional pedirle, o que la solicitud se dirija automaticamente al deposito nacional mas cercano
que tenga las vacunas solicitadas disponibles.

LISTAR SOLICITUDES E COMPRA DE VACUNAS
    *crear un tabla de solicitudes de compra de vacunas y que el usuario nacional que tiene acceso a las comprar pueda ver las solicitudes para ver todas las vacunas que se requieren,
        y, en caso de que no sea posible comprar todas, pueda decidir cual/es comprar dependiendo de cual/es son las mas requeridas.
    *cuando un deposito/centro vacunacion pide vacunas que no haya en stock, se guarde en la tabla de solicitudes el tipo de vacuna que se necesita.
    *funcionabilidad para ordenar la tabla por columna.
    *paginacion.

personalizar el mensaje se error al solicitar vacunas.

en los formularios de compra/solicitudes, que venga seleccionado el deposito/centro en que trabaja el usuario

refactorear los metodos enviar de httpREquest.js para que no sean ellos los que abren el modal mensaje.

crear un servicio que guarde un estado para las operaciones comunes.

refactorizar listadoLote.js y listadoSubLote.js porque hacen casi lo mismo